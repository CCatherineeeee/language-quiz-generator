# The demo reads the PROJECT-ROOT .env first (DATABASE_URL, GROQ_API_KEY already there),
# then this demo/.env as an override. You usually don't need this file at all.
#
# Add more free LLM providers here to extend the fallback chain. The provider
# registry is in app/llm/providers.py — uncomment the matching entry there, then
# set its key here.

# GEMINI_API_KEY=
# OPENROUTER_API_KEY=
# CEREBRAS_API_KEY=
